================================================================================
TASK 010-speakers-voice-api
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B010-speakers-voice-api.md
================================================================================
TASK 011-reviews-context-api
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B011-reviews-context-api.md
================================================================================
TASK 012-output-exports-notifications-api
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B012-output-exports-notifications-api.md
================================================================================
TASK 012A-output-export-api
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B012A-output-export-api.md
================================================================================
TASK 012B-notifications-api
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B012B-notifications-api.md
================================================================================
TASK 013-admin-sse-error-contracts
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B013-admin-sse-error-contracts.md
================================================================================
TASK 014-openapi-client-generation
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B014-openapi-client-generation.md
================================================================================
TASK 015-frontend-scaffolding-routing
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B015-frontend-scaffolding-routing.md
================================================================================
TASK 016-design-tokens-ui-primitives
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B016-design-tokens-ui-primitives.md
================================================================================
TASK 017-api-client-query-keys-errors
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B017-api-client-query-keys-errors.md
================================================================================
TASK 018-app-shell-navigation-i18n-telemetry
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B018-app-shell-navigation-i18n-telemetry.md
================================================================================
TASK 019-auth-session-ux
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B019-auth-session-ux.md
================================================================================
TASK 020-dashboard
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B020-dashboard.md
================================================================================
TASK 021-project-list
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B021-project-list.md
================================================================================
TASK 022-project-creation-wizard
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B022-project-creation-wizard.md
================================================================================
TASK 023-resumable-upload-ux
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B023-resumable-upload-ux.md
================================================================================
TASK 024-processing-start-preflight
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B024-processing-start-preflight.md
================================================================================
TASK 025-project-workspace-shell
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B025-project-workspace-shell.md
================================================================================
TASK 026-live-progress-sse-polling
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B026-live-progress-sse-polling.md
================================================================================
TASK 027-transcript-workspace
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B027-transcript-workspace.md
================================================================================
TASK 028-translation-workspace
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B028-translation-workspace.md
================================================================================
TASK 029-voice-assignment-preview
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B029-voice-assignment-preview.md
================================================================================
TASK 030-media-player-waveform-timeline
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B030-media-player-waveform-timeline.md
================================================================================
TASK 031-manual-review-studio
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B031-manual-review-studio.md
================================================================================
TASK 032-quality-control-interface
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B032-quality-control-interface.md
================================================================================
TASK 033-output-export-experience
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B033-output-export-experience.md
================================================================================
TASK 034-notifications-center
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B034-notifications-center.md
================================================================================
TASK 035-activity-cost-preferences
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B035-activity-cost-preferences.md
================================================================================
TASK 035A-activity-cost-ui
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B035A-activity-cost-ui.md
================================================================================
TASK 035B-settings-preferences-ui
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B035B-settings-preferences-ui.md
================================================================================
TASK 036-admin-diagnostics-ui
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B036-admin-diagnostics-ui.md
================================================================================
TASK 037-security-privacy-hardening
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B037-security-privacy-hardening.md
================================================================================
TASK 038-observability
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B038-observability.md
================================================================================
TASK 039-unit-component-mock-tests
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B039-unit-component-mock-tests.md
================================================================================
TASK 039A-test-infra-coverage
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B039A-test-infra-coverage.md
================================================================================
TASK 039B-frontend-state-matrices
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B039B-frontend-state-matrices.md
================================================================================
TASK 039C-backend-unit-gaps
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B039C-backend-unit-gaps.md
================================================================================
TASK 040-integration-cross-layer-tests
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B040-integration-cross-layer-tests.md
================================================================================
TASK 040A-cross-layer-harness
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B040A-cross-layer-harness.md
================================================================================
TASK 040B-cross-layer-seams
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B040B-cross-layer-seams.md
================================================================================
TASK 041-e2e-visual-a11y-perf
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B041-e2e-visual-a11y-perf.md
================================================================================
TASK 041A-journeys-smoke
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B041A-journeys-smoke.md
================================================================================
TASK 041B-visual-regression
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B041B-visual-regression.md
================================================================================
TASK 041C-accessibility
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B041C-accessibility.md
================================================================================
TASK 041D-performance
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B041D-performance.md
================================================================================
TASK 042-ci-contract-management
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B042-ci-contract-management.md
================================================================================
TASK 042A-basic-ci
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B042A-basic-ci.md
================================================================================
TASK 043-hosting-rollout-operations
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B043-hosting-rollout-operations.md
================================================================================
TASK 043A-hosting-env-cdn
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B043A-hosting-env-cdn.md
================================================================================
TASK 043B-rollout-rollback
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B043B-rollout-rollback.md
================================================================================
TASK 043C-runbooks-backup
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B043C-runbooks-backup.md
================================================================================
TASK 044-optional-enrichment-local-ai
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B044-optional-enrichment-local-ai.md
================================================================================
TASK 045-i18n-rtl-foundation
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B045-i18n-rtl-foundation.md
================================================================================
TASK 046-test-harness-fixtures
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B046-test-harness-fixtures.md
================================================================================
TASK 047-backup-policy-restore-verification
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B047-backup-policy-restore-verification.md
================================================================================
TASK 048-frontend-feature-flag-hook
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B048-frontend-feature-flag-hook.md
================================================================================
TASK 049-notification-channel-abstraction
================================================================================
You are a senior production engineer executing a single task from a sequential implementation plan. 
## Execution Protocol
1. Identify Context: Parse the `TARGET_TASK_FILE` path to extract the 3-digit task number ($CurrentNumber). Calculate the previous number ($PreviousNumber) and next number ($NextNumber).
2. Read Previous Report: List the `tasks_report_B/` directory. Find and read the report matching the previous task number. Apply its "Recommendations for Next Agent". If no previous report exists, this is task 001; proceed.
3. Read Target Task: Read the `TARGET_TASK_FILE` completely. Do NOT read any other task files in `execution_tasks_B/` or `implementation_plan-B.md`.
4. Execute: Implement the task exactly as specified.
   - Production-grade only. No TODOs, stubs, or "implement later" comments.
   - Do not over-implement (ignore future tasks).
   - Do not under-implement (fulfill every requirement, test, and validation command in the file).
   - Run all build and test commands specified in the task's "Validation" section. Fix all failures before finishing.
5. Write Report: Create `tasks_report_B/{current_number}-{task-name}.md` containing exactly these sections:
   - Status: COMPLETED | BLOCKED
   - Summary: 2-4 sentences of what was done.
   - Files Created/Modified: List with 1-line descriptions.
   - Decisions Made: Ambiguities resolved and why.
   - Build/Test Results: Exact command outputs (last 10 lines).
   - Recommendations for Next Agent ({next_number}): Current repo state, gotchas, naming conventions, incomplete integration points, test helpers, warnings, and config keys. Be highly specific (include exact file paths, class names, and method signatures).
## Absolute Rules
- Never ask questions. Make deterministic, production-safe decisions and document them in the report.
- Never use shell concatenation for FFmpeg/commands (always use `ArgumentList` or safe arrays).
- Never hardcode secrets; use configuration placeholders like `"CHANGE_ME"`.
- If a build or test fails, fix the root cause. Do not delete tests to make them pass.
---
TARGET_TASK_FILE: @execution_tasks_B049-notification-channel-abstraction.md
